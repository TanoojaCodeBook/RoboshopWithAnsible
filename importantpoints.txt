 catalogue module is created with nodejs and mongodb

extracted the catalogue.zip folder
package.json --> Defines project info
Lists required libraries (dependencies)
Defines how to start the app

server.js -->(Main application code)
Starts a web server
Connects to MongoDB
Exposes APIs (endpoints)

db-->master-data.js -->
master-data.js creates the catalogue database and inserts data

MySQL	             MongoDB
Database	         Database
Table	             Collection
Row	                 Document (JSON)

In catalogue.yaml sir preloaded the schema right ->because this preloaded data is required to run the app.
why sir ran this in catalogue.yaml server only. we can load data in mongodb server right by creating mangodb.yaml file?
Answer: .

👉 Yes, technically you can load data from the MongoDB server
But in real-world DevOps, we usually do what your sir did—and there’s a reason.
🧠 Two possible approaches
✅ Approach 1: Load from MongoDB server

(Create mongodb.yaml)

- name: Load data
  hosts: mongodb
  tasks:
    - shell: mongosh </app/master-data.js

👉 This works ✔️

✅ Approach 2: Load from Catalogue server (your sir’s way)
- name: Load data
  hosts: catalogue
  tasks:
    - shell: mongosh --host {{ MONGODB_HOST }} </app/db/master-data.js

👉 This is what your sir used ✔️

🔥 Why your sir chose catalogue server
✅ 1. Data belongs to the application(catalogue microservice/module)
master-data.js is part of catalogue service code

So:

App owns its data

👉 Best practice:

Keep DB scripts with the service that uses them

✅ 2. Loose coupling (very important)

If you load from MongoDB server:

MongoDB needs app files ❌ (malli mongodb server lo kuda master-data files download cheyyali)

✅ 3. Microservices design principle

In microservices:

Each service manages:
its schema
its data

👉 So:

catalogue service → loads catalogue DB

✅ 4. No need to copy files to DB server

If using MongoDB server:

You must copy master-data.js there ❌

Current approach:

File already exists in /app/db ✅

When deploying catalogue:

Deploy app → Load DB → Start service (for these we written script in ansible in catalogue.yaml)

👉 Everything happens in one place

Even though you run the script from the catalogue server, the data is stored in the MongoDB server
When you run:

mongosh --host {{ MONGODB_HOST }} </app/db/master-data.js

👉 This means:

Run command on catalogue server
Connect to MongoDB server  (mongosh --host {{ MONGODB_HOST }})
Execute script on that remote DB




# user (mongodb + redis) and car is connected to redis. but in none of these sir didn't write any code to
# # load the schema/db something because Redis doesn’t require any schema or initial data setup.
# #Redis data is created dynamically by the application, not preloaded
#  👉 Redis does NOT use schema at all
# So:
# No tables ❌
# No collections ❌
# No schema files ❌   
# 🔥 Why no schema in Redis?
# Because Redis is:
# Key → Value
# Example:
# SET cart:1001 "item1,item2"
# 👉 That’s it—no structure defined beforehand
# 🛒 What happens in cart service
# When app runs:
# User adds item
#         ↓
# Cart service writes to Redis
#         ↓
# Key is created automatically

# 👉 No need to pre-create anything

# 👤 What happens in user service
# User logs in
#         ↓
# Session stored in Redis

# 👉 Again:

# Key created dynamically (automatic ga values redis lo store aipothai. prestructer avasaram ledu)
# No schema needed

# User mongodb ki connect avuthundi mari data preload cheyyala vaddha?
# 🧠 Why catalogue why they loaded the data?
# Because:
# Product data must exist before app starts
# So they manually loaded it trhough script
#in real time how do they know preolaod reqeuired or not
# 👉 In real projects, they don’t “guess”
# They know by looking at requirements + application behavior + documentation
# Check project artifacts (what you did 👍)
# Engineers check repo:
# .sql / .js -->	preload required
# No DB scripts -->	runtime data
#user and cart application code lo .sql or .js script levu so no data preload is required for these two services
#and user account creation times aa details runtime lo mongodb and redis ki vellipothai(connection 

| Service   | Tech   | Process |
| --------- | ------ | ------- |
| catalogue | NodeJS | node    |
| user      | NodeJS | node    |
| shipping  | Java   | java    |
| payment   | Python | uwsgi   |
| cart      | NodeJS | node    |

uwsgi is a server used to run Python applications

👉 In Roboshop:

Payment service is written in Python
It runs using uwsgi
